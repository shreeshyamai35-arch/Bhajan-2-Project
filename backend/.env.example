# Copy to .env (never commit real keys) and export before running the app.
# Google Gemini API key with Lyria access (music generation).
# NOTE: Lyria requires a billing-enabled / paid tier. A free-tier key returns
# HTTP 429 "not enough quota" and the app will fall back to the mock instrumental.
BHAJAN_API_KEY=your-google-api-key-here

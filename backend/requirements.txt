numpy>=1.24
soundfile>=0.12
pyloudnorm>=0.1.1
# Optional (auto-used if installed): tempo/key detection
librosa>=0.10 ; python_version >= "3.10"
# Real music generation (only needed when GEMINI_API_KEY is set)
google-genai>=0.3 ; python_version >= "3.10"
